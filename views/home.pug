extends layout

block content
  h1 Home
  p.lead Quickly add assignments to the ProfessorBot student notifier system.
  hr
  .row
    .col-sm-6
      h2 Welcome
      p Quickly add assignment details and ProfessorBot will remind your classmates! By default ProfessorBot will remind all subscribed classmates three days in advance of any assignment once per day at noon on Discord and optionally by text.
      p
        a.btn.btn-default(href='#', role='button') View details »
    if user
      .col-sm-6
        h2 Add an Assignment
        form
          .form-group
            label(for="course") Course:
            select.form-control(id="course")
              option ITAS155
              option ITAS191
              option ITAS167
              option ITAS185
          .form-group
            label(for="assignmentName") Assignment name:
            input.form-control(type="text", id="assignmentName", placeholder="In Class Lab 4")
          .form-group
            label(for="dueDate") Due date:
            input.form-control(type="date", id="dueDate")
          .form-group
            label(for="dueTime") by what time:
            input.form-control(type="time", id="dueDate")
          .form-group
            label(for="url") Assignment URL:
            input.form-control(type="url", id="url")
          .form-group
            label(for="note") Special note (optional):
            textarea.form-control(type="textarea", rows="3", id="note")
          button.btn.btn-primary(type="submit") Submit
    else
      .col-sm-6
        h2 Let's get started
        p Login or sign up if you would like to edit assignment notifications for the class.
        p
          a.btn.btn-default(href='/login', role='button') Login »
          a.btn.btn-default(href='/signup', role='button') Create Account »